if [ -f /etc/bashrc ]; then
    . /etc/bashrc
fi
if [ -f /etc/skel/.bashrc ]; then
    . /etc/skel/.bashrc
fi
if [ -f ~/.bash_local ]; then
    . ~/.bash_local
fi

<%= render 'nvm.erb' %>

<%= render 'rbenv.erb' %>

<%= render 'alias.erb' %>

<%= render 'paths.erb' %>